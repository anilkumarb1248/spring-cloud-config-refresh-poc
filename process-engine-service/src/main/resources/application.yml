server:
  port: 2052

message: Welcome to Process Engine from local properties file
feature-flag: false

spring:
  application:
    name: ProcessEngine
  config:
    import: optional:configserver:http://localhost:2050

management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: health, refresh, bus-refresh, beans, env

